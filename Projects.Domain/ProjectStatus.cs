namespace Projects.Domain
{
    public enum ProjectStatus
    {
        New = 10,
        Started = 20,
        Paused = 30,
        Resumed = 40,
        Finished = 50,
        Cancelled = 100
    }
}